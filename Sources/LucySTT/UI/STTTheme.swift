import SwiftUI

enum STTTheme {
    static let text = Color.black
    static let textSecondary = Color.black.opacity(0.72)
    static let textMuted = Color.black.opacity(0.62)

    static let blush = Color(red: 1.0, green: 0.75, blue: 0.81)
    static let cream = Color(red: 1.0, green: 0.98, blue: 0.94)
    static let hotPink = Color(red: 1.0, green: 0.08, blue: 0.58)
    static let plum = Color(red: 0.35, green: 0.16, blue: 0.33)
}
